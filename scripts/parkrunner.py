import pandas as pd


class Parkrunner:
    def __init__(self, id: str) -> None:
        self.id = id

    def get_summary_results(country: str, id: str) -> pd.DataFrame:

        return

    def get_all_results(country: str, id: str) -> pd.DataFrame:
        return

    def get_location_results(country: str, location: str, id: str) -> pd.DataFrame:

        return
